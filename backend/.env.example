# Required env
NODE_ENV=development
PORT=4000

# Enforce testnet-first
NETWORK=amoy
CHAIN_ID=80002

# Secrets
JWT_SECRET=changeme
VAULT_CLUB_API_KEY=changeme
HMAC_SECRET=changeme

# External ST function bases (no trailing slash)
# Example: https://<project>.supabase.co/functions/v1
ST_API_AUTH=
ST_EXTERNAL_API=

# Optional CORS
FRONTEND_ORIGIN=http://localhost:5173

# Optional: enable/disable HMAC verification for local testing
HMAC_REQUIRED=true
